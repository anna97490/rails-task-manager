<br>
<h1>My tasks</h1>
<% @tasks.each do |task| %>
  <%= link_to task_path(task) do %>
    <div>
      <%= task.title %>
      <%= task.completed %>
    </div>
  <% end %>
<% end %>

<%= link_to "Create a new task", tasks_new_path(@task) %>
