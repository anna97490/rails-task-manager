<br>
<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>

<%= link_to "Edit task", edit_task_path(@task) %>
<%= link_to  "Erase task", task_path(@task), method: :delete, data: {confirm: "Are you sure ? "} %>
